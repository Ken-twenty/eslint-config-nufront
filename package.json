{
  "name": "eslint-config-nufront",
  "version": "1.0.6",
  "description": "GD-NUFRONT eslint config for Vue projects.",
  "main": "index.js",
  "scripts": {
    "init": "npm install",
    "release": "npm publish --access=publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ken",
  "license": "MIT",
  "dependencies": {
    "eslint-restricted-globals": "^0.2.0"
  },
  "engines": {
    "node": ">= 4"
  }
}
